package advent_of_code_2022.day_5;

import java.nio.file.Files;
import java.nio.file.Path;
import java.util.*;

public class Solution {
/*
    public static void main(String[] args) throws Exception {
        Solution s = new Solution();
        s.part1();
        //s.part2();
    }

    void part1() throws Exception {
        List<String> lines = Files.readAllLines(Path.of(getClass().getClassLoader().getResource("day_5t.txt").toURI()));

        Map<Integer, Queue<String>> parsed = new HashMap<>();

        boolean digitLine = false;
        for (int i = 0; i < lines.size(); i++) {
            char[] line = lines.get(i).toCharArray();

            for(int j = 0, k = 0; j < line.length; j += 4, k++) {
                if (Character.isDigit(line[j])) {
                    digitLine = true;
                    break;

            }

            if (digitLine) {
                break;
            }
        }
    }

    void part2() throws Exception {
        //List<String> lines = Files.readAllLines(Path.of(getClass().getClassLoader().getResource("day_4.txt").toURI()));

    }
    void
*/
}
